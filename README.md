# Prompt Dev

Create and refine prompt templates following Claude conventions.

## Usage

Invoke via Claude Code:

- "create a prompt"
- "build a template"
- "fix this prompt"
- "prompt isn't working"

## Workflow

DISCOVER → DRAFT → TEST → REFINE → VALIDATE

State persists in files for resumption across sessions.
